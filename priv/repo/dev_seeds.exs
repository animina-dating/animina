# Development seed data has been split into multiple files for maintainability.
# See priv/repo/dev_seeds/ for the individual modules:
#
#   personas.ex — 114 persona definitions with unique character descriptions
#   stories.ex  — unique intros (114), topic stories (20/topic), long stories
#   profiles.ex — 20 personality trait profiles
#   seeder.ex   — seeding logic (user creation, moodboards, traits, activity)
#
# The entry point is seeds.exs which loads all files from dev_seeds/.
